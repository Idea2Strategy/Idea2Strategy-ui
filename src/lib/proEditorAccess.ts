/*
  Pro authoring remains behind one explicit release switch while its API and
  validation flow are unfinished. Keeping the decision in one place prevents
  a new entry point from accidentally exposing the editor before it is ready.
*/
export const PRO_EDITOR_AVAILABLE = false;
